#include <iostream>
#include <cmath>

int main(int argc, char **argv) {
    std::cout << "Hello, world!" << std::endl;
    
    double something = sin(1.0);

    std::cout << something << std::endl;

    return 0;
}
